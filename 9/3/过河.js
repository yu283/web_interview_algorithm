function he(s) {
  let arr = s.split(' ')
  let newArr = arr.map((item) => {
      return parseInt(item)
  })
  let n = newArr.length
  for (let i = n - 1; i > 0; i--) {
    
  }
}
let arr = [1,2,3,4,5,6,7]
console.log(arr.lastIndexOf(3))
